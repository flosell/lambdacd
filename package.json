{
  "name": "lambdacd",
  "devDependencies": {
    "watch":"latest",
    "postcss-cli": "latest",
    "autoprefixer": "latest"
  },
  "scripts": {
    "build:css": "node_modules/postcss-cli/bin/postcss --use autoprefixer src/css/pipeline.css -d resources/public/css",
    "build": "npm run build:css",
    "build:watch": "watch 'npm run build' src/"
  }
}
